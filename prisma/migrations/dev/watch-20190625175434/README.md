# Migration `watch-20190625175434`

This migration has been generated by Mael Kerichard at 6/25/2019, 5:54:36 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."Guild"("id" TEXT NOT NULL  ,"discordId" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."User"("id" TEXT NOT NULL  ,"discordId" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Warn"("id" TEXT NOT NULL  ,"reason" TEXT NOT NULL DEFAULT '' ,"timestamp" DATE NOT NULL  ,"authorId" TEXT NOT NULL DEFAULT '' ,"target" TEXT NOT NULL  REFERENCES GuildUser(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."BadWord"("id" TEXT NOT NULL  ,"value" TEXT NOT NULL DEFAULT '' ,"guild" TEXT NOT NULL  REFERENCES Guild(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."GuildUser"("id" TEXT NOT NULL  ,"verified" BOOLEAN NOT NULL DEFAULT false ,"messages" INTEGER NOT NULL DEFAULT 0 ,"captchaText" TEXT   ,"opinion" TEXT   ,"guild" TEXT NOT NULL  REFERENCES Guild(id),"user" TEXT NOT NULL  REFERENCES User(id),PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190625175434
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource db {
+    provider = "sqlite"
+    url      = "file:dev.db"
+    default  = true
+}
+
+generator photon {
+    provider = "photonjs"
+    output   = "node_modules/@generated/photon"
+}
+model Guild {
+    id    String  @default(cuid()) @id @unique
+    discordId String @unique
+    createdAt DateTime
+    users GuildUser[]
+    badWords BadWord[]
+}
+model User {
+    id    String  @default(cuid()) @id @unique
+    discordId String @unique
+    guildUsers GuildUser[]
+}
+model Warn {
+    id    String  @default(cuid()) @id @unique
+    reason String
+    timestamp DateTime   @default(now())
+    authorId String @unique
+    target GuildUser
+}
+model BadWord {
+    id    String  @default(cuid()) @id @unique
+    value String @unique
+    guild Guild
+}
+model GuildUser {
+    id    String  @default(cuid()) @id @unique
+    user User
+    guild Guild
+    verified Boolean @default(false)
+    messages Int @default(0)
+    captchaText String?
+    warns Warn[]
+    opinion String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190625175434)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190625175434'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
