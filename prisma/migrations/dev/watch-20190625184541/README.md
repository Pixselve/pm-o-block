# Migration `watch-20190625184541`

This migration has been generated by Mael Kerichard at 6/25/2019, 6:45:42 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."new_Guild";

ALTER TABLE "lift"."Guild" ADD COLUMN "baseRoleAfterVerificationId" TEXT   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20190625182549..watch-20190625184541
--- datamodel.dml
+++ datamodel.dml
@@ -14,8 +14,9 @@
     createdAt DateTime
     users GuildUser[]
     badWords BadWord[]
     verificationRequired Boolean? @default(true)
+    baseRoleAfterVerificationId String?
 }
 model User {
     id    String  @default(cuid()) @id @unique
     discordId String @unique
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190625184541)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190625184541'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
